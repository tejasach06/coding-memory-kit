# Session start hook

Run:
```bash
scripts/memory-context --query "session start project context global preferences"
```
Inject output as context. Do not treat it as authoritative without verification.
